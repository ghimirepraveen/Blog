{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "concurrently \"cd server && npm install\" \"cd client && npm install\"",
    "client_build": "cd client && npm run build",
    "build": "npm run install && npm run client_build && cd server && npm run build",
    "start": "cd server && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "react-icons": "^5.2.1"
  }
}
